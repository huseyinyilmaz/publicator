{application, http, [
	{description, "Http interface implementation for publicator"},
	{vsn, "1"},
	{registered, []},
	{applications, [
		kernel,
		stdlib,
                lager,
                jiffy,
                gproc,
		cowboy,
		server
	]},
	{mod, {http_app, []}},
	{env, []}
]}.
